Django>=5.0,<6.0
djangorestframework>=3.15
requests>=2.31
python-dotenv>=1.0
psycopg[binary]>=3.1
gunicorn>=21.0
# Optional — only needed if Kafka verification is used by the test runner:
# kafka-python>=2.0
pytest>=8.0
pytest-django>=4.8
